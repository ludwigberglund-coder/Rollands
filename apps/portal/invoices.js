const app=document.getElementById('invoices-app');
const Demo=globalThis.RollandsDemoScenario;
const Invoice=globalThis.RollandsInvoice;
const Money=globalThis.RollandsMoney;
const Pdf=globalThis.RollandsInvoicePdf;
const isDemo=location.hostname.endsWith('github.io')||new URLSearchParams(location.search).has('demo');
let company={},view='list',draft=null,preview=null,previewRecord=null,busy=false,dirty=false,search='';
let privateCustomers=[],privateInvoices=[],csrfToken=sessionStorage.getItem('rollands-csrf')||'',issueReady=false,issueBlocker='',issueRequestId='';
const PRIVATE_DRAFT_KEY='rollands-private-invoice-draft-v1';
const copy=value=>JSON.parse(JSON.stringify(value));
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const amount=value=>Money.formatSek(Number(value||0));
const today=()=>new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Stockholm',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
const later=fn=>setTimeout(fn,0);
function plusDays(value,days){const d=new Date(value+'T12:00:00Z');d.setUTCDate(d.getUTCDate()+Number(days));return d.toISOString().slice(0,10);}
function url(path){return path+(isDemo?(path.includes('?')?'&':'?')+'demo=1':'');}
function customers(){if(!isDemo)return privateCustomers;const state=Demo.state(),map=new Map();for(const i of state.customerInvoices||[])if(!map.has(i.customerNumber))map.set(i.customerNumber,{customerNumber:i.customerNumber,name:i.customerName,paymentTermsDays:30});for(const c of state.customers||[])map.set(c.customerNumber,c);return [...map.values()];}
function accounts(){return Invoice.revenueAccounts(isDemo?(Demo.state().invoiceRevenueAccounts||[]):[]);}
async function api(path,options={}){const headers={'Accept':'application/json',...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})};if(options.method&&options.method!=='GET'){if(!csrfToken)throw new Error('Säkerhetskontrollen saknas. Logga in igen.');headers['X-CSRF-Token']=csrfToken;}const response=await fetch(`/api/v1${path}`,{credentials:'same-origin',...options,headers,body:options.body?JSON.stringify(options.body):undefined});const data=await response.json().catch(()=>({}));if(!response.ok){const error=new Error(data.error||'Begäran misslyckades.');error.code=data.code;error.status=response.status;throw error;}return data;}
function readPrivateDraft(){try{return JSON.parse(sessionStorage.getItem(PRIVATE_DRAFT_KEY)||'null')}catch{return null}}
function accountOptions(vatRate,value=''){const list=Invoice.accountsForVat(accounts(),Number(vatRate));return '<option value="">Välj intäktskonto</option>'+list.map(a=>`<option value="${esc(a.number)}" ${a.number===value?'selected':''}>${esc(a.number)} · ${esc(a.name)}</option>`).join('');}
function sellerFromCompany(){return {name:company.legalName||'',address:company.address?.full||'',orgNumber:company.orgNumber||'',vatNumber:company.vatNumber||'',phone:company.contact?.phone||'',email:company.contact?.email||'',website:company.website||'https://rollands.se',bankgiro:company.invoice?.bankgiro||'',taxStatus:company.invoice?.taxStatus||''};}
function blankLine(){return {description:'',quantity:'1',unit:'',unitPrice:'0,00',vatTreatment:'',vatRate:'25',revenueAccount:''};}
function normalizeDraft(value){value.seller=sellerFromCompany();value.invoiceDate=value.invoiceDate||today();value.postingDate=value.postingDate||today();value.paymentTermsDays=Number(value.paymentTermsDays??30);value.dueDate=value.dueDate||plusDays(value.invoiceDate,value.paymentTermsDays);value.ourReference=value.ourReference||'';value.yourReference=value.yourReference||'';value.notes=value.notes||'';value.useFees=Boolean(value.useFees);value.includeMessage=Boolean(value.includeMessage&&value.notes);value.lines=(value.lines?.length?value.lines:[blankLine()]).map(row=>({description:row.description||'',quantity:String(row.quantity??'1'),unit:row.unit||'',unitPrice:String(row.unitPrice??'0,00'),vatTreatment:row.vatTreatment||'',vatRate:String(row.vatRate??'25'),revenueAccount:row.revenueAccount||''}));value.administration={amount:value.administration?.amount||'0,00',vatRate:'25',revenueAccount:'3690'};value.freight={amount:value.freight?.amount||'0,00',vatRate:'25',revenueAccount:'3520'};return value;}
function freshDraft(){const saved=isDemo?Demo.state().invoiceDraft:readPrivateDraft()?.draft;if(!isDemo){const stored=readPrivateDraft();issueRequestId=stored?.requestId||crypto.randomUUID();}if(saved)return normalizeDraft(copy(saved));return normalizeDraft({customerNumber:'',buyer:{name:'',address:'',orgNumber:'',email:''},seller:sellerFromCompany(),invoiceDate:today(),postingDate:today(),dueDate:plusDays(today(),30),paymentTermsDays:30,currency:'SEK',ourReference:'',yourReference:'',notes:'',useFees:false,includeMessage:false,lines:[blankLine()],freight:{amount:'0,00'},administration:{amount:'0,00'}});}
function chooseCustomer(number){const c=customers().find(c=>c.customerNumber===number);draft.customerNumber=number;if(!c)return;draft.buyer={name:c.name||'',address:typeof c.address==='string'?c.address:(c.address?.full||''),orgNumber:c.orgNumber||'',email:c.email||''};draft.paymentTermsDays=Number(c.paymentTermsDays??30);draft.dueDate=plusDays(draft.invoiceDate,draft.paymentTermsDays);}
function get(name){return name.split('.').reduce((value,key)=>value?.[key],draft);}
function set(name,value){const parts=name.split('.');if(parts.length===1)draft[name]=value;else draft[parts[0]][parts[1]]=value;}
function field(label,name,options={}){const v=get(name)??'',required=options.required?'required':'',wide=options.wide?'wide':'';if(options.textarea)return `<label class="${wide}">${esc(label)}${options.required?' *':''}<textarea name="${name}" rows="${options.rows||3}" maxlength="${options.max||600}" ${required}>${esc(v)}</textarea></label>`;return `<label class="${wide}">${esc(label)}${options.required?' *':''}<input name="${name}" type="${options.type||'text'}" value="${esc(v)}" maxlength="${options.max||120}" ${required} ${options.type==='number'?'min="0" max="365" step="1"':''}></label>`;}
function readonlyField(label,value,wide=false){return `<label class="${wide?'wide':''}">${esc(label)}<input value="${esc(value||'')}" readonly aria-readonly="true"></label>`;}
function section(title,body,open=true){return `<details class="invoice-section" ${open?'open':''}><summary>${title}</summary><div class="invoice-section-body">${body}</div></details>`;}
function vatOptions(value){return [25,12,6,0].map(v=>`<option value="${v}" ${Number(value)===v?'selected':''}>${v} %</option>`).join('');}
function lineHtml(row,index){const required=(label,key)=>`<label>${label}<input data-row-field="${key}" value="${esc(row[key])}" ${['quantity','unitPrice'].includes(key)?'inputmode="decimal"':''} required maxlength="120"></label>`,treatments=Object.entries(Invoice.VAT_TREATMENTS||{}).map(([id,rule])=>`<option value="${esc(id)}" ${row.vatTreatment===id?'selected':''}>${esc(rule.label)}</option>`).join('');let rate=row.vatRate;try{if(row.vatTreatment)rate=String(Invoice.vatTreatmentRate(row.vatTreatment,draft.invoiceDate));}catch{}return `<div class="invoice-line-editor" data-row="${index}"><div class="invoice-line-head"><h3>Fakturarad ${index+1}</h3><button class="button ghost small" type="button" data-remove-row="${index}" ${draft.lines.length===1?'disabled':''}>Ta bort rad</button></div><div class="invoice-grid invoice-line-fields"><label class="description-field">Benämning *<textarea data-row-field="description" maxlength="1200" rows="2" required>${esc(row.description)}</textarea></label>${required('Antal *','quantity')}<label>Enhet<input data-row-field="unit" value="${esc(row.unit)}" maxlength="30"></label>${required('À-pris exkl. moms, SEK *','unitPrice')}<label>Typ av försäljning *<select data-row-field="vatTreatment" required><option value="">Välj typ</option>${treatments}</select></label><label>Momssats<input data-row-field="vatRate" value="${esc(rate)}" readonly aria-readonly="true"><small>% · styrs av vald typ och fakturadatum</small></label><label class="account-field">Intäktskonto *<select data-revenue-select data-row-field="revenueAccount" required>${accountOptions(rate,row.revenueAccount)}</select></label></div></div>`;}
function editor(){const list=customers();const fees=draft.useFees?`<div class="invoice-grid"><label>Fakturaavgift exkl. moms (SEK)<input name="administration.amount" inputmode="decimal" value="${esc(draft.administration.amount)}"></label>${readonlyField('Moms för fakturaavgift','25 %')}${readonlyField('Intäktskonto för fakturaavgift','3690 · Övriga sidointäkter',true)}<label>Frakt, kr exkl. moms (SEK)<input name="freight.amount" inputmode="decimal" value="${esc(draft.freight.amount)}"></label>${readonlyField('Moms för frakt','25 %')}${readonlyField('Intäktskonto för frakt','3520 · Fakturerade frakter',true)}</div>`:'<p class="invoice-help">Aktivera valet ovan om fakturaavgift eller frakt ska tas med.</p>';const message=draft.includeMessage?field('Meddelande på faktura','notes',{textarea:true,wide:true,max:3000}):'<p class="invoice-help">Aktivera valet ovan om ett meddelande ska visas på kundens faktura.</p>';return `<div class="invoice-toolbar"><div><span class="eyebrow">Fakturautställning</span><h2>Ny kundfaktura</h2><p>Endast uppgifter som behövs för kundfakturan och bokföringen visas. Stjärna markerar obligatoriska uppgifter.</p></div><button class="button ghost" data-action="list">Till fakturalistan</button></div><form id="invoice-form">${section('1. Mottagare & kund',`<div class="invoice-grid"><label class="wide">Kundnummer *<select name="customerNumber" required><option value="">Välj kund</option>${list.map(c=>`<option value="${esc(c.customerNumber)}" ${c.customerNumber===draft.customerNumber?'selected':''}>${esc(c.customerNumber)} · ${esc(c.name)}</option>`).join('')}</select></label>${field('Företagsnamn - kund','buyer.name',{required:true})}${field('Organisationsnummer - Kund','buyer.orgNumber')}${field('Fakturaadress, postnummer och ort - kund','buyer.address',{required:true,textarea:true,wide:true,max:500})}${field('Mottagarens E-post - kund','buyer.email',{type:'email'})}</div>`)}${section('2. Datum, referens och leverans',`<div class="invoice-grid">${field('Fakturadatum','invoiceDate',{required:true,type:'date'})}${field('Bokföringsdatum','postingDate',{required:true,type:'date'})}${field('Förfallodatum','dueDate',{required:true,type:'date'})}${field('Betalningsvillkor dagar','paymentTermsDays',{type:'number',required:true})}${field('Vår referens','ourReference')}${field('Er referens','yourReference')}<label class="wide">OCR / betalningsreferens<input value="Fakturanummer tilldelas vid bokföring" readonly aria-readonly="true"></label></div><p class="invoice-help">Fakturadatum och bokföringsdatum fylls i automatiskt men kan ändras. Förfallodatum räknas om från fakturadatum + betalningsvillkor. OCR låses alltid till fakturanumret.</p>`)}${section('3. Fakturarader och valfri intäktskontering',`<p class="invoice-help"><strong>Intäktskonton filtreras efter vald momssats.</strong> Ett konto med fel momskoppling kan varken väljas här eller bokföras i motorn. <a href="${url('./accounts.html')}" target="_blank" rel="noopener">Öppna kontoplan / lägg till eget intäktskonto</a>.</p>${draft.lines.map(lineHtml).join('')}<button class="button ghost" type="button" data-action="add-row">+ Lägg till fakturarad</button>`)}${section('4. Avgifter och öresutjämning',`<label class="wide checkbox-label"><input name="useFees" type="checkbox" ${draft.useFees?'checked':''}> Använd fakturaavgift och/eller frakt</label>${fees}<p class="invoice-help">Öresutjämning beräknas alltid automatiskt och visas separat på fakturan.</p>`)}${section('5. Avsändare, kontakt och betalningsuppgifter',`<div class="invoice-grid">${readonlyField('Avsändarens juridiska namn',draft.seller.name)}${readonlyField('Organisationsnummer',draft.seller.orgNumber)}${readonlyField('Adress, postnummer och ort',draft.seller.address,true)}${readonlyField('VAT-nummer / momsregistreringsnummer',draft.seller.vatNumber)}${readonlyField('Telefon',draft.seller.phone)}${readonlyField('E-post',draft.seller.email)}${readonlyField('Webbplats',draft.seller.website)}${readonlyField('Skattestatus',draft.seller.taxStatus)}${readonlyField('Bankgiro',draft.seller.bankgiro,true)}</div><p class="invoice-help">Uppgifterna hämtas centralt från företagsinformationen och ändras inte i fakturafönstret.</p>`,false)}${section('6. Meddelanden',`<label class="wide checkbox-label"><input name="includeMessage" type="checkbox" ${draft.includeMessage?'checked':''}> Lägg till meddelande på faktura</label>${message}`)}<div class="invoice-panel-footer"><div class="invoice-actions"><button class="button ghost" type="button" data-action="save-draft">Spara utkast</button><button class="button ghost" type="button" data-action="open-draft-pdf">Öppna faktura PDF</button><button class="button ghost" type="button" data-action="print-draft-pdf">Skriv ut faktura PDF</button><button class="button" type="submit" ${!isDemo&&!issueReady?'disabled':''}>Skapa och bokför faktura</button></div><span class="invoice-muted">Inget skickas till kund eller bank.</span></div></form>`;}
function readForm(){const form=document.getElementById('invoice-form');if(!form)return;for(const [name,value] of new FormData(form).entries())if(!['useFees','includeMessage'].includes(name))set(name,value);draft.useFees=Boolean(form.elements.useFees?.checked);draft.includeMessage=Boolean(form.elements.includeMessage?.checked);if(!draft.includeMessage)draft.notes='';draft.lines=[...form.querySelectorAll('[data-row]')].map(row=>Object.fromEntries([...row.querySelectorAll('[data-row-field]')].map(input=>[input.dataset.rowField,input.value])));draft.seller=sellerFromCompany();draft.administration.vatRate='25';draft.administration.revenueAccount='3690';draft.freight.vatRate='25';draft.freight.revenueAccount='3520';}
function input(){const value=copy(draft);value.seller=sellerFromCompany();value.lines=value.lines.map(r=>({...r,articleNumber:'',discountPercent:'0'}));if(value.useFees){const feeOre=Money.parseOre(value.administration.amount||'0',{allowNegative:false,label:'Fakturaavgift'});if(feeOre)value.lines.push({kind:'administration',description:'Fakturaavgift',quantity:'1',unit:'st',unitPrice:value.administration.amount,vatRate:'25',revenueAccount:'3690'});const freightOre=Money.parseOre(value.freight.amount||'0',{allowNegative:false,label:'Frakt'});if(freightOre)value.lines.push({kind:'freight',description:'Frakt',quantity:'1',unit:'st',unitPrice:value.freight.amount,vatRate:'25',revenueAccount:'3520'});}value.notes=value.includeMessage?value.notes:'';return value;}
function listHtml(){const records=isDemo?(Demo.section('customerInvoices')||[]):privateInvoices,filtered=records.filter(r=>(r.invoiceNumber+' '+r.customerName+' '+r.customerNumber).toLocaleLowerCase('sv-SE').includes(search.toLocaleLowerCase('sv-SE')));return `<div class="invoice-toolbar"><div><span class="eyebrow">Försäljning</span><h2>Kundfakturor</h2><p>Fakturor med sparade originaluppgifter, momsstyrda intäktskonton och kund-PDF.</p></div><button class="button" data-action="new">+ Ny kundfaktura</button></div><input class="invoice-search" type="search" aria-label="Sök fakturor" placeholder="Sök fakturanummer eller kund" value="${esc(search)}"><div class="sales-panel"><div class="table-scroll"><table class="sales-table"><thead><tr><th>Faktura / OCR</th><th>Kund</th><th>Fakturadatum</th><th>Förfallodatum</th><th>Belopp</th><th>Restbelopp</th><th>Åtgärd</th></tr></thead><tbody>${filtered.slice().reverse().map(r=>`<tr><td><b>${esc(r.invoiceNumber)}</b><br>${esc(r.ocr)}</td><td>${esc(r.customerName)}<br><small>${esc(r.customerNumber)}</small></td><td>${esc(r.invoiceDate)}</td><td>${esc(r.dueDate)}</td><td class="money">${amount(r.totalOre)}</td><td class="money">${amount(r.remainingOre)}</td><td><button class="button ghost small" data-preview="${esc(r.id)}">Visa / skriv ut</button></td></tr>`).join('')||'<tr><td colspan="7">Inga fakturor matchar sökningen.</td></tr>'}</tbody></table></div></div>`;}
function previewHtml(){const d=preview;return `<div class="invoice-toolbar"><div><span class="eyebrow">Granskning & utskrift</span><h2>Faktura ${esc(d.invoiceNumber)}</h2><p>OCR är låst till fakturanumret. Bokföringsdatum visas endast internt och inte på kundens PDF.</p></div><button class="button ghost" data-action="${previewRecord?'list':'edit'}">${previewRecord?'Till fakturalistan':'Tillbaka till utkast'}</button></div>${d.warnings?.length?`<div class="invoice-alert">${d.warnings.map(esc).join('<br>')}</div>`:''}<div class="invoice-preview-card"><h3>${esc(d.buyer.name)}</h3><div class="invoice-preview-meta"><p><b>Avsändare</b><br>${esc(d.seller.name)}<br>${esc(d.seller.address)}<br>Org.nr: ${esc(d.seller.orgNumber)}<br>VAT: ${esc(d.seller.vatNumber)}</p><p><b>Mottagare</b><br>${esc(d.buyer.name)}<br>${esc(d.buyer.address||'Adress saknas i äldre underlag')}<br>Fakturadatum: ${esc(d.invoiceDate)}<br>Förfallodatum: ${esc(d.dueDate)}<br>OCR: ${esc(d.ocr)}</p></div><div class="table-scroll"><table class="sales-table"><thead><tr><th>Benämning</th><th>Antal</th><th>Enhet</th><th>À-pris</th><th>Moms</th><th>Intäktskonto (internt)</th><th>Netto</th></tr></thead><tbody>${d.lines.map(r=>`<tr><td>${esc(r.description)}</td><td>${Money.formatQuantity(r.quantityMilli)}</td><td>${esc(r.unit)}</td><td class="money">${amount(r.unitPriceOre)}</td><td>${r.vatRate==null?'Ej specificerad':esc(r.vatRate)+' %'}</td><td><b>${esc(r.revenueAccount||'Saknas i äldre underlag')}</b><br><small>${esc(r.revenueAccountName||'')}</small></td><td class="money">${amount(r.netOre)}</td></tr>`).join('')}</tbody></table></div><div class="invoice-totals"><div><span>Belopp före moms</span><strong>${amount(d.netOre)}</strong></div><div><span>Total moms</span><strong>${amount(d.vatOre)}</strong></div><div><span>Öresutjämning</span><strong>${amount(d.roundingOre)}</strong></div><div><span>Att betala</span><strong>${amount(d.totalOre)}</strong></div></div></div><div class="invoice-panel-footer"><div class="invoice-actions"><button class="button" data-action="open-pdf">Öppna faktura PDF</button><button class="button ghost" data-action="print-pdf">Skriv ut faktura PDF</button></div></div>`;}
function render(){const banner=isDemo?'<div class="demo-banner"><b>Demo med fiktiva uppgifter.</b> Fakturor sparas i denna webbläsare. Inga riktiga utskick eller betalningar sker.</div>':issueReady?'<div class="demo-banner"><b>Skyddad fakturering.</b> Bokföring sker i företagets privata databas. Inget skickas automatiskt till kunden.</div>':`<div class="invoice-alert"><b>Bokföring spärrad.</b> ${esc(issueBlocker||'Företagets fakturauppgifter måste verifieras före bokföring.')}</div>`;app.innerHTML=`<div class="portal"><aside class="sidebar"></aside><section class="main"><header class="topbar"><div><h1>Kundfakturor</h1><p>Ekonomi / Fakturautställning</p></div><span class="user-chip">${esc(isDemo?'Demoföretag':(company.displayName||company.legalName||'Skyddad företagsmiljö'))}</span></header><main class="content invoice-workspace">${banner}<div id="invoice-alert" class="invoice-alert" role="alert" tabindex="-1" hidden></div>${view==='edit'?editor():view==='preview'?previewHtml():listHtml()}</main></section></div>`;if(busy)for(const b of app.querySelectorAll('button'))b.disabled=true;}
function notify(message){const box=document.getElementById('invoice-alert');if(box){box.textContent=message;box.hidden=false;box.focus();}}
async function outputPdf(doc=preview){return Pdf.createInvoicePdf(doc,{record:previewRecord||{}});}
async function openPdf(doc=preview,print=false){const archived=!isDemo&&previewRecord?.id&&doc===preview;if(archived){const href=`/api/v1/customer-invoices/${encodeURIComponent(previewRecord.id)}/pdf`;const tab=window.open(href,'_blank');if(!tab)throw new Error('Webbläsaren blockerade PDF-fönstret. Tillåt popup-fönster och försök igen.');tab.opener=null;if(print)setTimeout(()=>{try{tab.focus();tab.print();}catch{}},1400);return;}const tab=window.open('about:blank','_blank');if(!tab)throw new Error('Webbläsaren blockerade PDF-fönstret. Tillåt popup-fönster och försök igen.');tab.opener=null;try{const bytes=await outputPdf(doc),blob=new Blob([bytes],{type:'application/pdf'}),href=URL.createObjectURL(blob);tab.location.href=href;if(print)setTimeout(()=>{try{tab.focus();tab.print();}catch{}},1200);setTimeout(()=>URL.revokeObjectURL(href),120000);}catch(error){tab.close();throw error;}}
async function draftDocument(){readForm();const doc=Invoice.prepare(input(),{accounts:accounts(),requireVatTreatment:!isDemo});await Pdf.createInvoicePdf(doc);return doc;}
function refreshAccounts(){if(view!=='edit')return;for(const select of app.querySelectorAll('[data-revenue-select]')){const row=select.closest('[data-row]');if(!row)continue;const vat=row.querySelector('[data-row-field="vatRate"]')?.value||'25',value=select.value,html=accountOptions(vat,value);if(select.innerHTML!==html)select.innerHTML=html;if(![...select.options].some(o=>o.value===value))select.value='';}}
document.addEventListener('input',event=>{if(event.target.closest('#invoice-form'))dirty=true;if(event.target.matches('.invoice-search')){search=event.target.value;later(()=>{render();const field=app.querySelector('.invoice-search');field?.focus();});}});
document.addEventListener('change',event=>{if(!event.target.closest('#invoice-form'))return;readForm();dirty=true;if(event.target.name==='customerNumber'){chooseCustomer(event.target.value);later(render);return;}if(['paymentTermsDays','invoiceDate'].includes(event.target.name)){try{draft.dueDate=plusDays(draft.invoiceDate,draft.paymentTermsDays);for(const row of draft.lines)if(row.vatTreatment)row.vatRate=String(Invoice.vatTreatmentRate(row.vatTreatment,draft.invoiceDate));}catch{}later(render);return;}if(event.target.dataset.rowField==='vatTreatment'){const rowIndex=Number(event.target.closest('[data-row]')?.dataset.row),treatment=event.target.value;draft.lines[rowIndex].vatTreatment=treatment;draft.lines[rowIndex].revenueAccount='';try{draft.lines[rowIndex].vatRate=String(Invoice.vatTreatmentRate(treatment,draft.invoiceDate));}catch{draft.lines[rowIndex].vatRate='';}later(render);return;}if(['useFees','includeMessage'].includes(event.target.name))later(render);});
document.addEventListener('click',async event=>{const target=event.target.closest('button');if(!target||busy)return;const action=target.dataset.action;try{if(target.dataset.preview){if(isDemo){previewRecord=(Demo.section('customerInvoices')||[]).find(i=>i.id===target.dataset.preview);preview=Invoice.documentFor(previewRecord,company);}else{const detail=await api('/customer-invoices/'+encodeURIComponent(target.dataset.preview));if(!detail.document)throw new Error('Fullständigt fakturaunderlag saknas för den här äldre fakturan.');previewRecord=detail.invoice;preview=detail.document;}view='preview';render();return;}if(target.dataset.removeRow!==undefined){readForm();draft.lines.splice(Number(target.dataset.removeRow),1);dirty=true;render();return;}if(action==='new'){draft=freshDraft();const selected=new URLSearchParams(location.search).get('customer');if(selected)chooseCustomer(selected);view='edit';render();return;}if(action==='list'){if(dirty&&!confirm('Lämna det osparade utkastet? Spara utkast först för att behålla ändringarna.'))return;dirty=false;view='list';render();return;}if(action==='edit'){view='edit';render();return;}if(action==='add-row'){readForm();draft.lines.push(blankLine());dirty=true;render();return;}if(action==='save-draft'){readForm();if(isDemo)Demo.patch(s=>{s.invoiceDraft=copy(draft);});else sessionStorage.setItem(PRIVATE_DRAFT_KEY,JSON.stringify({draft:copy(draft),requestId:issueRequestId||crypto.randomUUID()}));dirty=false;notify('Utkastet är sparat i den här inloggningssessionen. Det är inte bokfört.');return;}if(action==='open-draft-pdf'||action==='print-draft-pdf'){busy=true;await openPdf(await draftDocument(),action==='print-draft-pdf');return;}if(action==='open-pdf'||action==='print-pdf'){busy=true;await openPdf(preview,action==='print-pdf');return;}}catch(error){notify(error.message);}finally{busy=false;for(const button of app.querySelectorAll('button'))button.disabled=button.hasAttribute('data-remove-row')&&draft?.lines.length===1;}});
document.addEventListener('submit',async event=>{if(event.target.id!=='invoice-form')return;event.preventDefault();if(busy)return;try{readForm();const value=input();busy=true;for(const button of app.querySelectorAll('button'))button.disabled=true;const preflight=Invoice.prepare(value,{accounts:accounts(),requireVatTreatment:!isDemo});await Pdf.createInvoicePdf(preflight);if(isDemo){let created;Demo.patch(state=>{created=Invoice.postDemoInvoice(state,value);state.invoiceDraft=null;});previewRecord=created;preview=created.document;}else{if(!issueReady)throw new Error(issueBlocker||'Företagets fakturauppgifter måste verifieras före bokföring.');if(!issueRequestId)issueRequestId=crypto.randomUUID();const payload={requestId:issueRequestId,customerNumber:value.customerNumber,invoiceDate:value.invoiceDate,postingDate:value.postingDate,dueDate:value.dueDate,paymentTermsDays:value.paymentTermsDays,ourReference:value.ourReference,yourReference:value.yourReference,notes:value.notes,lines:value.lines};const created=await api('/customer-invoices',{method:'POST',body:payload});previewRecord=created.invoice;preview=created.document;privateInvoices=privateInvoices.filter(row=>row.id!==created.invoice.id);privateInvoices.unshift(created.invoice);sessionStorage.removeItem(PRIVATE_DRAFT_KEY);issueRequestId='';}view='preview';dirty=false;render();}catch(error){notify(error.message);}finally{busy=false;for(const button of app.querySelectorAll('button'))button.disabled=button.hasAttribute('data-remove-row')&&draft?.lines.length===1;}});
addEventListener('focus',()=>later(refreshAccounts));addEventListener('storage',()=>later(refreshAccounts));addEventListener('beforeunload',event=>{if(dirty){event.preventDefault();event.returnValue='';}});
async function boot(){if(isDemo){const response=await fetch('../content/company.json');if(!response.ok)throw new Error('Företagsinställningarna kunde inte hämtas.');company=await response.json();render();return;}const sessionResponse=await fetch('/api/v1/session',{credentials:'same-origin',cache:'no-store'});const session=await sessionResponse.json();if(!session.authenticated){location.href='./index.html';return;}if(!csrfToken){location.href='./index.html';return;}const [customerData,invoiceData,configData]=await Promise.all([api('/customers'),api('/customer-invoices'),api('/customer-invoices/config')]);privateCustomers=customerData.customers||[];privateInvoices=invoiceData.invoices||[];company=configData.company||{};issueReady=configData.issuanceReady===true;issueBlocker=configData.blocker||'';render();}
boot().catch(error=>{app.innerHTML=`<div class="portal"><aside class="sidebar"></aside><main class="content"><h1>Fakturaverktyget kunde inte laddas</h1><p>${esc(error.message)}</p></main></div>`;});
